# dependencies
node_modules
.pnp
.pnp.js

# next.js — anchor output dirs to the app root so they don't match route
# segments named build/dist/out (e.g. app/(app)/studies/[id]/build/).
.next/
/out/
/build
/dist

# environment
.env
.env*.local
# Deploy bootstrap secrets (ADR-0016). The .example variant stays tracked;
# the real .env.production (API keys owner pastes) must never be committed.
.env.production

# testing
coverage
playwright-report
test-results

# misc
.DS_Store
*.pem
.vercel
.idea
.vscode
*.tsbuildinfo
next-env.d.ts

# editor
.cursor

# Local TLS certs for dev:https (never commit private keys)
certificates/
