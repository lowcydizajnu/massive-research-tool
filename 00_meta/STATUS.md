# STATUS — Massive Research Tool

> Plain-markdown mirror of `00_meta/dashboard.html`. Updated by Claude whenever work happens. If this and the dashboard disagree, the dashboard is the visual; this file is the canonical source of truth that travels with the repo.

**Last updated:** 2026-06-02
**Current focus (2026-06-02):** **Phase 5 (Build) — MVP code-complete; hardening (owner-directed A→B→C).** **First fix (owner-spotted): the UI was rendering in Times, not IBM Plex.** Cause: the scaffold used `next/font/google` (fetches fonts from Google at build time → fails with no CDN, empty font vars) + a self-referential `@theme` font var. Fixed per spec (`tokens.md` says use `@fontsource`, no CDN): `layout.tsx` imports self-hosted `@fontsource/ibm-plex-*`; `tokens.css` font vars use literal family names; `@theme` fonts mirror the colors' bridge pattern. **Verified in the compiled production CSS** (`--font-sans` → IBM Plex Sans stack at `:root`); the sandbox preview can't paint the app (Clerk CDN), so **owner to confirm live**. 50 vitest + 4 e2e green, typecheck + build clean. Remaining hardening: route-aware top-bar Save + breadcrumb, full WAI-ARIA tablists, mutation role-enforcement, security-review, axe a11y; then post-MVP surfaces + deploy prep. Open MVP closeout: loop e2e green on real Clerk + live click-through of block editing/save.

**Previous focus (2026-06-02):** MVP authoring loop CODE-COMPLETE; first QA audit log + no-skipped-tests fix. Closed two discipline items: wrote the **first QA audit log** (`06_qa/audit-logs/2026-06-02-mvp-authoring-loop.md`) — honestly a QA *status*, **not a clean ship pass** (50 vitest + 4 surface e2e + typecheck/build green, but the loop e2e is unverified, no a11y/perf pass on authenticated surfaces, block editing + save not clicked live); and fixed a `qa-and-testing` violation — the gated loop e2e showed as a skipped test in main, so it moved into an **opt-in `auth` Playwright project** (`npm run test:e2e:auth`); default `test:e2e` is now **4 passed / 0 skipped**. **To close V1-MVP (owner-side):** run the loop e2e green on a real Clerk instance, a manual live click-through of block editing + save, an axe pass per authenticated surface — each logged as a follow-up in the audit file. **Post-MVP backlog** (deferred V1.5+): Preregister/Share/Run/Results, Whiteboard, Replications/Activity/Participants/Team, OSF push, AI substrate, recruitment; DB-backed Frameworks + curator authoring; the ADR-0012 optimistic-concurrency counter.

**Previous focus (2026-06-02):** every ADR-0011 MVP capability exists in code; the full Hanna-loop e2e is written (gated). The loop end to end: auth → Studies → New study (Blank or **from the Misinformation Research Framework**) → Builder (block add/remove/configure + validation + autosave) → **Save as named version**. Final piece: `e2e/hanna-loop.spec.ts` (`@clerk/testing`) drives sign-in → from-Framework → configure → save named → reopen-persists, **gated behind `RUN_AUTH_E2E=1`** (skips in this sandbox — no Clerk CDN — runs on your machine/CI). **Verified: 50 vitest + 4 surface e2e green (loop e2e skipped), typecheck + build clean.** The loop e2e itself is **unverified** (sign-in strategy/selectors best-effort). **To close V1-MVP:** run the loop e2e green on a real Clerk instance + a manual live click-through of block editing & save, then a QA audit-log entry.

**Earlier (2026-06-02):** **ADR-0011 MVP authoring loop works on Blank + from-Framework paths (Framework registry).** Shipped the Misinformation Research Framework as an **in-repo curated definition** (`server/frameworks/registry.ts`: social-post stimulus + pre-worded likert-7 manipulation check); DB-backed `Framework`/`FrameworkVersion` deferred to curator authoring (`data-model/03-framework-entities.md`). `frameworks.list` feeds the New-study modal's Framework picker (enabled); `studies.create({kind:"framework"})` copies the framework's blocks into the new study's autosave (fresh ULIDs, preset configs, locks derived per ADR-0012). **QA: 50 vitest + 4 e2e green, typecheck + build clean.** **Remaining for full MVP:** the **full Hanna-loop e2e** (`@clerk/testing`, env-gated — skips in this sandbox, green on your machine/CI).

**Earlier (2026-06-02):** **ADR-0011 MVP authoring loop code-complete (Blank path) — Save-as-version dialog.** Signup → onboard → `/studies` → New study → Builder (add + configure blocks, autosaved) → **Save as named version**. The Save-as-version dialog landed (`save-as-version-dialog.md`): `studies.saveAsNamed` snapshots the autosave working tip into a new immutable `named` ExperimentVersion (label-unique, next version_number) per ADR-0012, autosave continues; dialog offers autosave / named (default) / review-deferred, advisory (non-blocking) warning for incomplete blocks, saved toast. **QA: 48 vitest + 4 e2e green, typecheck + build clean.** **Remaining for full MVP:** the full **Hanna-loop e2e** (`@clerk/testing` — green on your machine, not this sandbox) and the **Misinformation Research Framework seed** (item 9, unlocks the "from Framework" start). Block editing + save are test-covered but not yet clicked in a real browser.

**Earlier (2026-06-02):** **ADR-0011 MVP Builder functional (items 7 + 8 — module picker + block CRUD).** Block editing landed per ADR-0012: Module/ModuleVersion tables + migration `0001` + seed of `core/social-post@1.0.0` + `core/likert-7@1.0.0` (on Neon); in-repo registry = runtime Zod source of truth (DB stores the JSON-Schema mirror). `modules.list` feeds the picker; `studies.get` returns typed blocks; `studies.addBlock/removeBlock/updateBlockConfig` mutate the autosave working tip (Zod-validated at write, `module_version_locks` synced, LWW per ADR-0012). The Builder UI (`components/feature/builder/*`) is the interactive three-zone body: StageTabs + work surface (editable title + block list with Ready/Needs-setup badges + module-picker popover) + right panel that flips Details ↔ Configure on selection. **QA: 46 vitest + 4 e2e green, typecheck + build clean.** The authoring loop is buildable end to end. **Next: Save-as-version dialog** (named-save snapshots the autosave), then the **full Hanna-loop e2e** (`@clerk/testing`), then the **Misinformation Research Framework seed** (item 9).

**Earlier (2026-06-02):** **ADR-0011 MVP item 7 (Builder); block-editing architecture locked (ADR-0012), implementing.** The full MVP loop was **verified live in a real browser** (signup → onboard → `/studies` → New study → Builder → rename title → reload persists). **ADR-0012 accepted** (block format = typed `{instanceId: ULID, source, key, version, config}` array validated against `ModuleVersion.schema` via Zod at write; the single `autosave` version is the **mutable working tip**, named/preregistered/published are immutable snapshots from it — a clarification of ADR-0002; LWW concurrency in V1) + data-model entry `02-module-entities.md` (Module + ModuleVersion). **Now implementing straight through** (owner approved, no checkpoints): Module/ModuleVersion tables + migration + seed (`core/social-post@1.0.0`, `core/likert-7@1.0.0`) → module picker (item 8) + block add/remove/validate → Save-as-version dialog. Pipeline green throughout (41 vitest + 4 e2e + typecheck + build).

**Earlier (2026-06-02):** **ADR-0011 MVP item 7 in progress: Builder editing (tRPC client + editable title).** Wired the tRPC **HTTP client + React Query** (`lib/trpc/react.tsx`: `createTRPCReact<AppRouter>` + `TRPCReactProvider` with `httpBatchLink` to `/api/trpc`, no transformer; mounted in the `(app)` layout) — the client-mutation half of tRPC. First consumer: the **editable study title** (`EditableStudyTitle` client island → `studies.updateTitle`, tenant-scoped; title lives on Experiment so no version churn). RSC pages still read via the in-process caller. **QA: 41 vitest + 4 e2e green, typecheck + build clean.** **Next (the big remaining piece): block editing** — needs a data-model/ADR decision on the block-instance shape + autosave-vs-immutability (ADR-0002 clarification), then `Module`/`ModuleVersion` tables (new migration) + seed, then the module picker (item 8) + block add/validate, then the Save-as-version dialog; then the Framework seed (item 9) + the full Hanna-loop e2e.

**Earlier (2026-06-02):** **ADR-0011 MVP item 7 started: the Builder shell (read-only).** `studies.get` (tenant-scoped query, NOT_FOUND outside the workspace) feeds the Build stage. The `(app)` layout was refactored to stop wrapping children in one card — each surface composes the row itself (Studies = one card; Builder = three zones), faithful to the wireframes. Build stage (`build-stage-builder-mode.md` v0.5.3): floating `StageTabs` pill (Build active, rest inert) + work-surface card (static Plex Serif title, version/stage/edited subtitle, Builder/Whiteboard toggle with Whiteboard inert, Blocks section with the blank empty state + inert `+ Add block`) + right Details panel. **Read-only shell** — title editing, block add (module picker, item 8), drag-reorder, validation, and autosave land in the next unit (which wires the tRPC **HTTP client + React Query**). **QA: 39 vitest + 4 e2e green, typecheck + build clean.** **Next: the Builder editing unit.**

**Earlier (2026-06-02):** **ADR-0011 MVP items 5-6 landed (Studies destination + New study modal); auth/signup (step 2) complete before them.** New study: `studies.create` tRPC mutation (Experiment + v1 + `current_version_id` in one transaction) via a thin `createStudyAction` server action (no tRPC HTTP client yet); a `role=dialog` modal opened from a shared `NewStudyProvider` context (TopBar button + empty-state CTA → one modal); Blank functional → creates a draft → `/studies/[id]/build` (Builder placeholder, item 7); Framework/Template disabled per the wireframe's own no-Frameworks edge case (seed is item 9). Study cards now link to Build; `/studies/new` removed. **QA: 37 vitest + 4 e2e green, typecheck + build clean.** **Next: Builder mode (item 7)** — the three-zone block surface per `build-stage-builder-mode.md`, which finally pulls in the tRPC **HTTP client + React Query** for block edits/autosave; then Module picker + Save dialog, seeding the Misinformation Research Framework, and the full Hanna-loop e2e.

**Earlier (2026-06-02):** **ADR-0011 MVP item 5 (Studies destination) landed; auth/signup (step 2) complete before it.** The Studies destination is the first authenticated surface: `app/(app)/layout.tsx` is the canonical modular shell (floating `TopBar` cap over `[LeftRail · work-surface card]`) that resolves the active workspace via tRPC and bounces a not-yet-onboarded user to `/signup`; `app/(app)/studies/page.tsx` (RSC via the in-process caller) renders the `Studies` header + sub-nav filter tabs + study cards or the empty state per `studies-destination.md`. `+ New study` routes to a `/studies/new` placeholder (modal is next). `middleware.ts` now redirects unauthenticated users on protected routes to **our** `/signin` (not Clerk's hosted page); post-onboarding + sign-in land on `/studies`. **QA: 34 vitest + 4 Playwright e2e green, typecheck + build clean.** (Authenticated chrome visuals aren't screenshot-verifiable in the sandbox — headless preview can't load Clerk's client SDK — so eyeball in a real browser; protection + list logic are test-covered.) **Next: New study modal** (Framework/Template/Blank → create-draft mutation; needs the tRPC HTTP client + React Query), then Builder mode + seeding the Misinformation Research Framework, then the full Hanna-loop e2e.

**Earlier (2026-06-02):** **ADR-0011 step 2 COMPLETE: all 4 small honest commits landed (data-model gate ✓ → Drizzle ✓ → Clerk ✓ → signup ✓).** Commit 4 built the signup onboarding flow: `05_app/app/(auth)/` holds the custom signup/signin/verify/sso-callback pages per the `signup-onboarding` wireframe (centered parchment column, NO Clerk prebuilt UI — built on Clerk client hooks, the deliberate `(auth)`-surface exception). Email magic-link OR Google OAuth → display name + theme (reused `ThemeToggle`, now with arrow-key nav) → workspace name → `server/onboarding/finalize.ts` (one postgres-js transaction: user upsert + workspace + owner member; then theme/onboarding metadata via the adapter). `ThemeProvider` stays Clerk-free (persists via a `set-theme` server action; accepts a server-seeded `initialChoice`); `app/page.tsx` shows an authenticated welcome (read via the adapter). **Vendor pin: `@clerk/nextjs` locked to v6.39.5** — v7.4.x routes hooks through `@clerk/react`'s experimental signals API, which diverges from the documented classic API; v6 keeps the classic `{ isLoaded, signUp, setActive }` + `createEmailLinkFlow`/`authenticateWithRedirect` (server adapter unaffected). QA: **27 vitest tests + 3 Playwright e2e green**, typecheck + production build clean, adapter discipline verified by grep (only the 3 inventoried exceptions). **Next: STATUS suggested-move #3** — Playwright + the full Hanna-loop e2e (needs a seeded Clerk session + the Studies destination to land on), then build the **Studies destination** (tRPC `studies.list` + the three-zone modular layout per `studies-destination.md`).

**Previous focus (2026-06-01):** **Phase 5 (Build) — ADR-0011 step 2 underway as 4 small honest commits (data-model gate → Drizzle → Clerk → signup).** Project owner finished the setup checklist (GitHub repo created, Clerk + Neon keys in `05_app/.env.local`, scaffold verified). This session added the `origin` remote (HTTPS — no SSH keys on the Mac) and pushed all three existing commits; `main` tracks `origin/main`. **Commit 1 landed (docs gate):** cleared the phase-gate prerequisite `00-core-entities.md` left open by deferring the auth/tenancy entities. New `04_architecture/data-model/01-auth-tenancy-entities.md` defines User / Workspace / Member and locks the load-bearing decision **Workspace IS the tenant** (the sketch's `tenant_id` resolves to `workspace.id`; no separate Tenant entity), with Clerk as identity-only and membership in our Drizzle `member` table (not Clerk Orgs) per ADR-0007 — so an auth-vendor migration only re-points `user.external_id`, never the authz graph. `hasCompletedOnboarding` + `themeChoice` + `lastWorkspaceId` live in Clerk `publicMetadata`, not user columns. New `02_product/feature-specs/signup-and-onboard.md` is the feature-spec gate artifact (ADR-0011 step (c)), linking the flow + wireframe + ADR-0007/0011 + the new data model. Vendor APIs pre-verified against live Clerk v7 + Drizzle docs to avoid hallucinated APIs. Validator clean at 38. **Next: commit 2** — Drizzle bootstrap (`drizzle.config.ts`, `server/db/schema.ts` for user/workspace/member/experiment/experiment_version + enums + CHECKs, `postgres-js` client for interactive transactions — not `neon-http`), first generated migration, hermetic `pglite` migration test.

**Previous focus (2026-05-29):** **Phase 5 (Build) advancing — adapter discipline in place before Clerk wires.** Project owner asked for fresh auth-provider comparison; chose to stay with Clerk for V1 with explicit instruction "be prepared to migrate — maybe it won't happen but just not to be blocked and locked." Mechanical preparation landed: `05_app/server/adapters/auth.ts` defines the typed `AuthAdapter` interface (identity primitives only — workspace membership lives in Drizzle, not Clerk Orgs); narrow `AuthUserMetadata` shape exposes theme preference + last-workspace hint so `ThemeProvider` can swap localStorage-only → Clerk-synced without component changes; `Proxy`-based `auth` export throws on first call until `auth.clerk.ts` lands. `05_app/server/adapters/README.md` documents the one rule (vendor SDK imports live in this folder only). Strategic preparation also landed: `04_architecture/lock-in-inventory.md` tracks Clerk + Vercel + Liveblocks + Inngest + OSF + AI providers with per-vendor exception columns; ADR-0007 amended (2026-05-29) to set Better Auth as the updated migration target (was Auth.js) — TypeScript-first, organizations primitive built in, first-class Drizzle. Phase 5 commits so far: app skeleton (theme + tokens + verification page) + adapter discipline (interface + inventory + ADR amendment). **Next commit:** install `@clerk/nextjs`, implement `auth.clerk.ts` against `AuthAdapter`, middleware protecting `(app)/*` routes, `/signup` route per `signup-onboarding` wireframe, and first Drizzle migration for Workspace/Member/Experiment/ExperimentVersion. Substrate: IA v0.3 + brief v0.5.1 + new flow + new JTBD. Four wireframe specs at draft: Studies destination, New study modal, Build stage Builder mode, Save as version dialog. Two of the four visualized in chat side-by-side Light + Dark (Studies destination + Build stage Builder mode) to exercise the v0.5.1 commitment that Light + Dark ship in V1. Final parchment tone still explicitly held open — wireframes now provide the in-context view the project owner asked for before deciding. Phase-gate sequence honored: first JTBD (`build-a-study`) + first user flow (`hanna-build-a-study`) authored before wireframes per `CLAUDE.md`. Validator clean (25 instances, +6 since v0.5.1).

## Overall

- Artifacts done: 25 Foundation + 14 Product + 11 Architecture + 4 Research + 14 Design = 68 (+ 2 Build commits)
- **Open quality compromise (logged honestly):** all four personas rest on synthetic-pilot evidence + literature, not field interviews. Confidence is `medium` for the literature-validated claims, `low` for the segmentation-specific claims. Each persona's "Open questions / what to validate" section names specific revisit triggers — most are "before V1 ships" or "before any high-stakes design decision depending on this persona."
- Active phase: Product (Phase 2) + Architecture (Phase 4) + Research (Phase 1) running in parallel
- Positioning RESOLVED: Qualtrics + GitHub for research, combined.
- Module architecture RESOLVED: versioned modular composition with theme overlays (ADR-0001).
- Forking model RESOLVED: snapshot-based, pull-from-upstream deferred but forward-ready (ADR-0002).
- **Persona evidence base STRENGTHENED**: literature confirms tool fragmentation, OSF integration as credibility floor, misinformation-tooling demand, and Prolific dominance for recruiting; some persona claims (modern-UX-as-credibility, "computing-power" specifics) still need interview confirmation.

---

## Phase pipeline

| # | Phase          | Done / Total | Status                                      |
| - | -------------- | ------------ | ------------------------------------------- |
|   | Foundation     | 19 / 19      | Complete                                    |
| 0 | Discovery      | 0 / 0        | Optional, recurring — open when needed      |
| 1 | Research       | 4 / 4        | 2 insights + interview guide + pilot notes  |
| 2 | Product        | 14 / 14      | 4 personas + product brief + 2 JTBDs + 5 user flows |
| 4 | Architecture   | 10 / 10      | ADR-0001-0007 + 0009 + 0011 + data model    |
| 3 | Design         | 14 / 14      | Brief v0.6 + IA v0.3 + inspiration + 9 wireframe specs + tokens |
| 5 | Build          | in progress  | Scaffold + auth/signup (ADR-0011 step 2) + tRPC + Studies destination |
| 6 | QA             | in progress  | Test strategy + first audit log (MVP, status-not-pass) |

---

## Foundation (complete)

Root docs: `README.md`, `CLAUDE.md`, `PROCESS.md`, `STACK.md`, `00_meta/STATUS.md`.

Rules in `00_meta/rules/`: `architecture.md`, `code-quality.md`, `qa-and-testing.md`, `design-rules.md`, `research-rules.md`, `decision-records.md`.

Templates in `00_meta/templates/`: `ADR-template.md`, `user-flow-template.md`, `user-journey-template.md`, `wireframe-spec-template.md`, `feature-spec-template.md`, `research-protocol-template.md`, `experiment-spec-template.md`, `pr-checklist-template.md`. Generated from the manifest — do not hand-edit.

Manifest system in `00_meta/manifest/`: `schema.yaml` (source of truth for 23 artifact types), `_schema.py` (shared parsing/rendering), `regenerate.py` (rebuilds templates from manifest), `validate.py` (checks instances + template drift), `new_artifact.py` (scaffolds a new instance), `requirements.txt`, `README.md`.

Phase folders: all created with READMEs and `.gitkeep` markers in empty subfolders. Archive folder (`99_archive/`) ready.

---

## Phase 1 — Research

No artifacts yet. Expected types when work begins:

- Literature notes → `01_research/literature/`
- Protocols → `01_research/protocols/`
- Insights → `01_research/insights/`
- Research decisions → `01_research/decisions/`

## Phase 2 — Product

In progress.

- `product-brief.md` — raw vision dump from project owner (and others). Captured 2026-05-27. **Status: raw brainstorm, not validated.** Contains 12 themes, scope analysis, ADR triggers, decomposition plan.
- Personas → `02_product/personas/`
  - `principal-investigator.md` — Dr. Maya Okonkwo, academic PI in misinformation research. **Status: in review.** Synthetic-pilot + literature triangulated.
  - `postdoc-operator.md` — Dr. Hanna Kowalczyk, solo postdoc + daily tool operator. Synthetic-pilot, low confidence.
  - `multi-site-coordinator.md` — Dr. Marek Stein, consortium coordinator (63 labs). Synthetic-pilot; highest WTP signal but explicitly fictional.
  - `burned-replicator.md` — Dr. Sofia Marsh, reproducibility-driven reformer. Synthetic-pilot; flagged as least representative (convert by injury), most valuable for warnings.
  - **Still to draft (deferred):** Participant, External Collaborator, Framework Contributor, Org Admin.
- Jobs-to-be-done → `02_product/jobs-to-be-done/`
  - `build-a-study.md` — first JTBD; anchored on Hanna, validated against Maya's review role. Drives the build-a-study flow.
- User flows → `02_product/user-flows/`
  - `hanna-build-a-study.md` — first user flow; happy path (Framework / Template / Blank → Builder → Save), three decision points, five failure modes, Mermaid diagram. Status: draft.
- User journeys → `02_product/user-journeys/` (blocked on flows — multiple flows needed to compose a journey)
- Use cases → `02_product/use-cases/` (blocked on flows)

## Phase 3 — Design

Started 2026-05-28; advancing.

- Inspiration → `03_design/inspiration/`
  - `README.md` — intake convention.
  - `2026-05-28-first-batch.md` — six annotated references (Maze, Notion, TheyDo, Typeform Build/Logic/Modal); three-zone layout pattern, builder-vs-whiteboard duality, right-drawer + live-preview combinations.
- `design-language-brief.md` — **v0.5.3**. Stage-tabs placement re-locked from Option A (v0.5.2 Typeform pattern, top-bar center slot) to **Option B (Miro / FigJam pattern, floating pill above center work surface only)** after the project owner saw both in context. Better matches the brief's modular-floating-panels spirit: top bar stays slim with global chrome only (workspace + breadcrumb left; ⌘K + Save when on a study + user avatar right); stage-tabs pill spans only the center column width and floats over the work surface card it controls; side rails extend full height of the center column. Three modules on a parchment board with a slim top hat — clean modular reading. Layout system + Top-bar navigation sections rewritten. Build-stage-builder-mode wireframe spec updated to match. All v0.5.1 commitments (warm parchment, modular panels, Plex Serif promotion, Light/Dark with full token tables, Themeability) preserved.
- *(superseded v0.5.1 description retained in changelog of `design-language-brief.md`.)* IBM Plex Serif/Sans/Mono locked. Three refinements to v0.5: (a) parchment paled — `surface.page #F4EFE6 → #F7F2E8`, `surface.panel #FBF7EE → #FCFAF3` after project-owner feedback "too creamy"; final tone held open for confirmation alongside first wireframes; (b) **dark mode upgraded from post-V1 to V1 ship** — Light + Dark + System (auto) all available with user toggle persisted per user; every neutral / border token now has Light + Dark values (dark surfaces are warm-dark, not cool slate, to preserve editorial tone); (c) **Themeability section added** — tokens are CSS variables, themes are bundles of token values, Light + Dark + System for users, internal exploration themes for design discipline (live in `03_design/design-system/themes/`, never reach production, dev-mode switch). Hard rules: no raw hex in components, every new color decision adds both Light and Dark values in the same change, every screen contrast-checked in both modes before handoff. Possible ADR-0010 (theming system) flagged but deferred until Build — implementation is straightforward (CSS vars + ThemeProvider + persisted user pref). v0.5 modular floating panels + Plex Serif in more roles + v0.4 functional vibrancy all preserved.
- Wireframes → `03_design/wireframes/`
  - `studies-destination.md` — Hanna's home base; list of studies, sub-nav, `+ New study` entry. Draft.
  - `new-study-modal.md` — first decision point (Framework / Template / Blank). Draft.
  - `build-stage-builder-mode.md` — the meat; full three-zone modular surface with stage tabs, study title in Plex Serif, block list with status badges, right context panel tabs. Draft.
  - `save-as-version-dialog.md` — third decision point (Continue autosaving / Save as named / Save & request review). Draft.
- `ia/information-architecture.md` — **v0.3** with five new conceptual picks locked (on top of v0.2 picks). (1) **Forks live as a relationship, not a destination** — surface in three places: forker's Studies list with `Replicating {parent-author} · {parent-study}@v` subtitle + new `Replicating` sub-nav, parent's right-panel `Replications` tab (two-directional family tree, renamed from "Lineage"), Activity stream events. No top-level "Forks" left-rail destination. (2) **Templates revoked as a Frameworks alias** — they're different concepts. Templates moves to `Library · Templates` as its own sub-section; Frameworks stays top-level as curated systems (schema + modules + measurement + reporting conventions for a research tradition like "Misinformation Research Framework"). (3) **Activity gets a `Yours / Follows` split** — Follows is the retention surface, fed by five follow targets: tag, author, Framework, study, saved search; `+ Follow` affordance on every tag chip, author byline, Framework details tab, study details tab, and inside the search modal. (4) **Participants destination structured as five sub-views** — Panels (saved cohorts, anon IDs), Open recruitment (provider status via RegistryAdapter), Compensation (payouts/invoices), Quality (flagged sessions awaiting decision), Connections (provider OAuth). Aggregated only; PII never surfaces. (5) **Template share scopes** — four scopes (Public-replicable default per ADR-0002 / Workspace / Invite link / Submit to Framework); single `Share` panel per saved version, no multi-step publish wizard. v0.2 picks still apply (Linear workspace switcher, scoped global search, comments on both surfaces, no bell, six stages with Preregister as its own). Object-typed right-panel tab system. Builder/Whiteboard toggle in study top-bar. Studies flat (tags do grouping). Mobile collapses rail to bottom tab bar; whiteboard read-only on mobile.
- Wireframes, design system, prototypes, handoff → next.

## Phase 4 — Architecture

Started (parallel to Product Phase 2).

- ADRs → `04_architecture/adrs/`
  - **`0001-modular-composition-theme-overlays.md` — accepted 2026-05-27.** Versioned module identity (`source/key@version`), first-class schemas, pure-functional theme overlays, fork-version-locking, plugin-ready data model. Question-type granularity for modules.
  - **`0002-forking-model.md` — accepted 2026-05-27.** Stripped Option B: snapshot-based forking, pull-from-upstream deferred. Versions are immutable with kinds (autosave, named, preregistered, published). Forks reference a specific parent version. Participant data never copied. Public-forkable default. Frameworks share the model. Forward-readiness checklist for upgrade to full B.
  - **`0003-asset-storage.md` — accepted 2026-05-28.** Option B: hybrid (researcher choice per asset between internal R2 upload and external link), auto-freeze on preregistration/publication with per-asset opt-out, failed freezes flag the version honestly. Per-tenant storage metering architected; pricing model deferred. Content-hash dedup for internal assets.
  - **`0004-preregistration-amendments.md` — accepted 2026-05-28.** Amendments to preregistered versions follow OSF / ClinicalTrials.gov convention: a new `kind: preregistered` version with `supersedes_version_id` + required `change_summary`. Both versions remain public, immutable, queryable. No silent edits. New fields added to ExperimentVersion in the data-model entry.
  - **`0005-osf-integration.md` — accepted 2026-05-28.** Push-to-OSF on preregistration. Option B sub-B.3: JSON snapshot + rendered PDF + OSF template-field mapping + materials, pushed asynchronously via Inngest. OAuth per-user (researchers own their OSF identity), default-on with per-registration opt-out, async with retry, failed pushes flag the version honestly (never block preregistration). **Registry-agnostic adapter interface** so AsPredicted / ClinicalTrials.gov / etc. plug in later via the same shape. Three new entities sketched (Registry, RegistryConnection, RegistryPush).
  - **`0006-ai-plugin-architecture.md` — accepted 2026-05-28.** Task-based AI architecture with AIProviderAdapters (mirrors RegistryAdapter from ADR-0005). Schemas-first output validation directly enabled by ADR-0001 module schemas. Per-tenant routing with sensitivity-aware policy enforcement (`metadata_only` / `experiment_definition` / `participant_data` / `pii`). Append-only `ai_invocation` audit log + per-tenant cost metering. Caching for deterministic tasks. Human-in-the-loop UX default. V1 ships substrate only; no AI features. Four new entities sketched.
  - **`0007-path-a-vs-b.md` — accepted 2026-05-28.** Path A with adapter discipline. Vercel + Clerk + Liveblocks + Inngest; each behind a typed adapter (`HostingShape` / `AuthAdapter` / `RealtimeAdapter` / `BackgroundJobAdapter`). No vendor-specific types in application code; no Clerk pre-built UI components in production; lock-in inventory + code-review rule enforce the discipline. Cost-ceiling triggers committed: $200/mo plan, $500/mo execute per-piece. Migration order when triggered: Clerk → Auth.js, then Liveblocks → Yjs, then Inngest → BullMQ, hosting last.
  - **`0009-supply-side-strategy.md` — accepted 2026-05-28.** Three-layer hybrid addressing the biggest non-technical risk. Layer 1 (now, mostly done): default virtue as continuous architectural posture — every UX decision should make the path of least resistance produce a better-documented artifact. Layer 2 (V1 GTM): demand-first targeting consortia, replicators, open-science labs; explicitly NOT trying to convert reluctant authors. Layer 3 (V2+ partnerships): mandate leverage via selected journals (Royal Society Open Science, BMJ titles, PLOS) and funders (NIH, NSF, Wellcome, DFG); gated by V1 product evidence. Back catalogue is explicitly out of scope — flow > stock.
  - **Up next:** the architecture queue is substantively complete (9 ADRs). Phase 3 (Design) is fully unblocked. ADR-0008 (plugin model, deferred from ADR-0001) is queued but not blocking.
  - Queued: `0008-plugin-model` (deferred).
- Diagrams → `04_architecture/diagrams/` (none standalone; Mermaid ER diagrams embedded in `data-model/00-core-entities.md`)
- Data model entries → `04_architecture/data-model/`
  - `00-core-entities.md` — Module, ModuleVersion, Theme, Experiment, ExperimentVersion + Framework note + Fork-as-relationship. Sketch, not migration. Resolves 5 ADR contradictions.
  - `01-auth-tenancy-entities.md` — **added 2026-06-01.** Resolves the auth/tenancy deferral from `00-core-entities.md`. User / Workspace / Member; locks "Workspace IS the tenant" (sketch's `tenant_id` → `workspace.id`); Clerk identity-only, membership in our DB not Clerk Orgs (ADR-0007); `hasCompletedOnboarding`/theme/lastWorkspace in Clerk `publicMetadata`, not columns. Grounds the first Drizzle migration (commit 2).
- API contracts → `04_architecture/api-contracts/` (none yet)

## Phase 5 — Build

**Started 2026-05-29.** Scaffolded per [ADR-0011](../04_architecture/adrs/0011-scaffold-strategy.md).

- `05_app/` — Next.js 15 + React 19 + TypeScript + Tailwind v4. First commit lands the app skeleton + the full v0.6 token system + a working theme toggle + a verification page exercising every visual primitive (parchment, Plex Serif, modular cards, vibrant validation badges, three-card theme picker). Second commit lands the AuthAdapter discipline before Clerk wires. See `05_app/README.md` for install + run.
  - `app/layout.tsx` — wraps everything in `ThemeProvider`; loads IBM Plex Sans/Serif/Mono via `next/font/google`.
  - `app/page.tsx` — temporary verification page; goes away once `/studies` becomes the default landing.
  - `app/globals.css` — Tailwind v4 + `@theme inline` bridge exposing every token as a utility class; `@custom-variant dark` driven by `[data-theme="dark"]`.
  - `styles/tokens.css` — canonical Light + Dark CSS variables; 1:1 with `03_design/design-system/tokens.md`.
  - `components/theme-provider.tsx` — three-mode picker (light/dark/system), localStorage today + Clerk metadata next iteration, listens for OS theme changes when `system` is active.
  - `components/theme-toggle.tsx` — three-card picker UI matching `signup-onboarding` + `account-settings` wireframes.
  - `lib/utils.ts` — `cn()` helper.
  - `server/adapters/auth.ts` — typed `AuthAdapter` interface; identity primitives only; theme + last-workspace via narrow `AuthUserMetadata`; `Proxy` default export throws until `auth.clerk.ts` lands next commit.
  - `server/adapters/README.md` — the rule: vendor SDK imports live in this folder only; PR review enforces.
  - `components.json` (shadcn-cli config), `.env.example`, `.gitignore`, `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`.
- `04_architecture/lock-in-inventory.md` — per-vendor breakdown (Clerk, Vercel, Liveblocks, Inngest, OSF, AI providers) with what's behind the adapter, deliberate exceptions, migration target, and cost-ceiling trigger. PR review consults this file when touching adapters.
- ADR-0007 amended 2026-05-29: Clerk migration target shifts from Auth.js → **Better Auth** (TypeScript-first, organizations primitive built in, first-class Drizzle integration). All other terms (cost ceilings, migration order, adapter discipline) unchanged.
- **Data layer (commit 2, 2026-06-02):** `05_app/server/db/` — `schema.ts` (Drizzle: `user`/`workspace`/`member`/`experiment`/`experiment_version` + 5 enums + CHECKs from the data model + unique indexes), `client.ts` (Drizzle over **postgres-js**, `prepare:false` — interactive transactions, not `neon-http`), `migrations/0000_quiet_vampiro.sql` (generated by drizzle-kit, **applied to Neon**), `__tests__/migration.test.ts` (hermetic pglite, 9 green). `drizzle.config.ts` loads `.env.local`. Scripts: `db:generate` / `db:migrate` / `test`.
- **Auth wiring (commit 3, 2026-06-02):** `05_app/server/adapters/auth.clerk.ts` implements `AuthAdapter` against Clerk (v6) — the only file importing `@clerk/nextjs/server` (identity mapping, session from `sessionClaims`, `signOut` via session revoke, narrow `publicMetadata` read/write, typed `UnauthorizedError`). `auth.ts` retired the throwing Proxy (`auth = clerkAuthAdapter`) and added `hasCompletedOnboarding` to `AuthUserMetadata`. `app/layout.tsx` wrapped in `ClerkProvider`; `middleware.ts` protects `/studies|/library|/frameworks|/settings` (auth pages public). `lock-in-inventory.md` Clerk row now lists three deliberate exceptions (ClerkProvider, clerkMiddleware, `(auth)` client hooks). 12 adapter unit tests green; adapter discipline verified by grep.
- **tRPC data layer (2026-06-02):** `05_app/server/trpc/` — tRPC v11 over a Context that gets identity via the `AuthAdapter`; `protectedProcedure` (auth + local `dbUser`) and `workspaceProcedure` (resolves the active workspace = tenant). Routers `workspace.active` + `studies.list` (tenant-scoped; stage from current version kind; sub-nav filters). Consumed via an in-process RSC caller (`server/trpc/server.ts`) + an `/api/trpc` fetch handler. zod inputs. 7 router tests (scoping + guards); 34 vitest total green. Deps: `@trpc/server` v11, `zod`.
- **Signup onboarding (commit 4, 2026-06-02):** `05_app/app/(auth)/` — `layout.tsx` (centered parchment column) + custom `signup`/`signin`/`signup/verify`/`signup/sso-callback` pages (no Clerk prebuilt UI; magic-link via `createEmailLinkFlow`, Google via `authenticateWithRedirect`, callbacks via the headless `handleEmailLinkVerification`/`handleRedirectCallback` hooks). `server/onboarding/finalize.ts` (one `postgres-js` transaction: user upsert + workspace + owner member; metadata via adapter). `app/actions/set-theme.ts` (durable theme persist); `ThemeProvider` extended (Clerk-free) + `ThemeToggle` arrow-key nav; `app/page.tsx` authenticated welcome. `e2e/signup-slice.spec.ts` + `playwright.config.ts` (3 green). **`@clerk/nextjs` pinned to v6.39.5** (v7.4.x's new signals API diverges from the documented classic hooks). 27 vitest + 3 e2e green; typecheck + build clean.

## Phase 6 — QA

- `06_qa/test-strategy.md` — complete
- `06_qa/audit-logs/2026-06-02-mvp-authoring-loop.md` — first audit log. QA status at MVP code-complete; honestly **not** a clean ship pass (loop e2e unverified, a11y/perf not run on authenticated surfaces, block-edit/save not clicked live). Lists the three follow-ups to close before V1 ships.

---

## Suggested next moves

1. **Create GitHub repo + add remote + push baseline.** On your end: create an empty private repo at `github.com/<your-username>/massive-research-tool` (no README/license/.gitignore — we have those). Then run from the project root:
   ```bash
   cd "/Users/pawelrosner/Dropbox/Mac/Documents/Claude/Projects/Massive Research Tool"
   git remote add origin git@github.com:<your-username>/massive-research-tool.git
   git push -u origin main
   ```
   (Use the HTTPS URL `https://github.com/...` instead if you don't have SSH set up. The first push asks you to authenticate.)
2. **Switch to Code tab in the Claude desktop app.** Pre-paste your `.env.local` with Clerk publishable key + secret key + Neon `DATABASE_URL`. Then start the session with: *"Continue from STATUS.md. Next move is the Clerk + Drizzle + signup commit per ADR-0011 step 2. Keys are in 05_app/.env.local."*
3. **Set up Playwright + write the failing MVP e2e first** per `qa-and-testing.md`. Single spec covers Hanna's whole loop: signup → study from Framework → 2 blocks → Save as named version → return tomorrow, find draft.
4. **Build the Studies destination** once the failing e2e exists. tRPC `studies.list` router + Drizzle query + the three-zone modular layout per `studies-destination.md` wireframe.
5. **Then in order**: New study modal → Builder mode (one block) → Module picker popover → Save dialog. Each gated by an e2e assertion. Misinformation Research Framework seeded with two starter blocks.
6. **Post-MVP wireframes (V1.5+)** deferred: Whiteboard mode, Replications tab populated, Activity Yours/Follows, Preregister stage, Share stage, Run stage, Results stage, Participants five sub-views, Team destination.
7. **Open IA questions queued** — cross-workspace participant registry, AI features home (Settings · AI), follow-target notification cadence (Settings · Notifications). Resolve when MVP-adjacent surfaces are built.
8. **ADR-0008 (plugin model)** — deferred architectural ADR. Wait for a real third-party plugin author to design for.
9. **ADR-0012 candidate (participant runtime + 3rd-party analytics)** — surfaced 2026-05-29 by project-owner question about Clarity / heatmap support. Researcher-facing app is SPA-like (fine — no third-party analytics on admin surfaces). Participant runtime is deferred to V1.5 per ADR-0011; when we design it, **build as server-rendered multi-page navigation** — `/take/[studyId]/[questionIndex]` per-question routes, real page transitions, no SPA on the participant side — so researchers can use Microsoft Clarity / Hotjar / FullStory / PostHog cleanly (same reason Qualtrics + SurveyMonkey render survey responses page-by-page). Add a researcher settings page to paste a third-party tracking ID; server-inject the script only on `/take/*` routes, never on the dashboard. GDPR consent flow for participant tracking needs product-level resolution. Don't write the ADR yet — design properly when V1.5 participant work starts.

---

(Previous suggested next moves preserved for reference)

Persona work parked (synthetic-pilot evidence only; revisit triggers logged per persona). Architecture queue continues:

1. **ADR-0005 (OSF integration).** Naturally next on the queue. Pulls together ADR-0003's freeze + ADR-0004's amendments + the open-science workflow that all four personas treat as a credibility floor.
2. **Phase 3 (Design) can start.** Personas + brief + 4 ADRs + data-model sketch are enough to begin IA and first wireframes. The Postdoc + PI personas are the right anchors for V1.
3. **Draft the first JTBDs.** Brief lists 6 candidates; the four personas now give us distinct viewpoints to map JTBDs against.
4. **ADR-0009 (supply-side strategy) when the time is right.** Surfaced by the segmentation insight as the biggest non-technical risk. Not blocking other work, but blocking confident go-to-market.

**Standing trigger to revisit personas:** before any high-stakes design decision rests on persona claims, or before V1 ships — whichever comes first.

**Project owner action (still pending):** share the link to the prior Claude-built quick-analysis dashboard, so the V1 stats scope has a concrete calibration target.

---

## Recent activity

- **2026-06-02** — **hardening: enforce member role on write mutations.** `workspaceProcedure` now resolves + attaches the caller's role; new `writeProcedure` rejects `viewer`-role members (FORBIDDEN) while reads stay open to any member. Applied to `studies.create`/`updateTitle`/`addBlock`/`removeBlock`/`updateBlockConfig`/`saveAsNamed`. Closes the QA-audit role-enforcement gap (no functional change for V1 single-author owners; enforces the boundary for when invites land). **51 vitest** (+1 viewer read-ok/write-FORBIDDEN) + 4 surface e2e green; typecheck + build clean.
- **2026-06-02** — **fix(fonts): UI rendered in Times, not IBM Plex (owner-spotted).** Two causes: the scaffold used `next/font/google` (downloads fonts from Google at build/dev time → fails with no CDN, leaving `--font-plex-*` empty) and `globals.css` `@theme` had a self-referential `--font-sans: var(--font-sans)` with no backing value. `tokens.md` already prescribed `@fontsource` (no CDN) — the scaffold had drifted. Fix: `layout.tsx` imports self-hosted `@fontsource/ibm-plex-{sans,serif,mono}` weights (replacing `next/font/google`); `tokens.css` `--font-{serif,sans,mono}` use literal family names (`"IBM Plex Sans"`, …); `@theme` fonts use the same inline bridge as the colors. **Verified in the compiled production CSS** (`--font-sans` → the IBM Plex Sans stack at `:root`, same structure as the working colors); the sandbox preview can't paint the app (`ClerkProvider` needs Clerk's CDN), so **owner to confirm live in a real browser**. 50 vitest + 4 surface e2e green; typecheck + production build clean. First item of the owner-directed hardening pass.
- **2026-06-02** — **qa: first QA audit log + fixed a "no skipped tests in main" violation.** Wrote `06_qa/audit-logs/2026-06-02-mvp-authoring-loop.md` — the first audit log, honestly a QA **status** not a clean ship pass: records 50 vitest + 4 surface e2e + typecheck/build green and the real-Postgres (PGlite, no DB mocks) integration coverage, plus the explicit **gaps** — loop e2e unverified, no a11y/perf pass on authenticated surfaces, mutation role-enforcement not done (single-author OK), block editing + save not clicked live; sign-off accepts risk for continued dev, **not** for shipping V1, and lists three follow-ups. Also fixed a `qa-and-testing.md` violation: the gated loop e2e had shown as "1 skipped" in the default run. Moved it from `test.describe.skip` into a dedicated **opt-in `auth` Playwright project** (the default `chromium` project `testIgnore`s it) + `npm run test:e2e:auth`. Default `npm run test:e2e` is now **4 passed / 0 skipped**; typecheck clean.
- **2026-06-02** — **test(e2e): full Hanna-loop spec (`@clerk/testing`) — the last ADR-0011 MVP item, written + gated.** `e2e/hanna-loop.spec.ts` drives sign in → New study from the Misinformation Research Framework → configure the stimulus (Needs-setup → Ready) → Save as named version → reopen and confirm persistence. Added `@clerk/testing` + `e2e/global-setup.ts` (`clerkSetup`) via `playwright.config` `globalSetup`. **Gated behind `RUN_AUTH_E2E=1`** — global-setup no-ops and the spec uses `test.describe.skip` without the flag, so the default suite is unchanged (**1 skipped, 4 surface e2e pass**; typecheck clean). Needs a Clerk session the sandbox can't establish (no CDN) → runs on your machine/CI; the spec header documents the test-user + env (`E2E_CLERK_IDENTIFIER`/`PASSWORD`) and flags the sign-in strategy/selectors as best-effort/**unverified**. With this, every ADR-0011 MVP capability exists in code (auth → Studies → New study [Blank/Framework] → Builder block CRUD + validation + autosave → Save as named version), all unit/integration-tested + typecheck + build green. **To close V1-MVP:** run the loop e2e green on a real Clerk instance + a manual live click-through of block editing & save; then a QA audit-log entry per the QA rule.
- **2026-06-02** — **feat(frameworks): Misinformation Research Framework + create-from-framework (ADR-0011 item 9).** Shipped the framework as an **in-repo curated definition** (`server/frameworks/registry.ts`) — V1 has one built-in and no curator-authoring flow, so DB-backed `Framework`/`FrameworkVersion` are deferred (recorded in `data-model/03-framework-entities.md`; upgrade path is a feature-add since `studies.create` already consumes a `{blocks}` definition). The framework = a social-post stimulus (empty — researcher fills it) + a pre-worded likert-7 manipulation check. `frameworks.list` feeds the New-study modal's Framework card (now enabled, embedded picker per `new-study-modal.md`); `studies.create` gained `kind:"framework"` + `frameworkKey`, copying the framework's blocks into the new study's autosave with fresh ULIDs + preset configs (`module_version_locks` derived per ADR-0012). **QA: 50 vitest** (+2: create-from-framework copies both starter blocks with fresh ids + likert arrives complete; unknown-framework → BAD_REQUEST) + 4 Playwright e2e green; typecheck + production build clean. The MVP authoring loop now works on both Blank and from-Framework paths. Next (last MVP item): the full Hanna-loop e2e via `@clerk/testing`, env-gated (skips in sandbox, green on your machine/CI).
- **2026-06-02** — **feat(builder): Save-as-version dialog — the MVP authoring loop is code-complete (`save-as-version-dialog.md`, ADR-0012).** `studies.saveAsNamed` snapshots the autosave working tip into a new immutable `named` ExperimentVersion (copies `definition_snapshot` + `module_version_locks`, next `version_number`, label unique within the study → CONFLICT otherwise); the autosave continues unchanged — ADR-0012's working-tip-vs-snapshot model. UI (`save-version-dialog.tsx`): centered modal, three radio options (Continue autosaving / Save as named version [default, label required] / Save & request review [disabled — Share stage deferred]), advisory non-blocking warning when blocks need setup, inline duplicate-label error; a Save button in the Builder title row opens it, transient success toast confirms. **Deviation (recorded):** incomplete-block completeness is advisory, not a hard save-block (a draft checkpoint may be WIP) — differs from the wireframe's blocking validation row. **QA: 48 vitest** (+2 saveAsNamed: snapshot leaves the tip intact, duplicate-label CONFLICT) + 4 Playwright e2e green; typecheck + production build clean. The full ADR-0011 MVP loop now exists end to end on the Blank path. **Remaining:** full Hanna-loop e2e (`@clerk/testing`; green on your machine/CI, not this sandbox) + the Misinformation Research Framework seed (item 9). Block editing + save are test-covered, not yet clicked live.
- **2026-06-02** — **feat: Builder block editing — module catalogue + picker + block CRUD (ADR-0011 items 7–8, ADR-0012).** Two commits. **(1) `feat(db)`:** Module + ModuleVersion tables + migration `0001` (applied to Neon) + an in-repo registry (`server/modules/registry.ts`) holding runtime **Zod** config schemas + metadata + `defaultConfig` + JSON-Schema mirror for `core/social-post@1.0.0` + `core/likert-7@1.0.0`; `server/db/seed-core.ts` (idempotent) + `db:seed` (tsx) seeded Neon; seed + migration tests. **(2) `feat(builder)`:** `modules.list`; `studies.addBlock`/`removeBlock`/`updateBlockConfig` mutating the autosave working tip's `definition_snapshot` (config Zod-validated at the write boundary, `module_version_locks` recomputed, LWW per ADR-0012); `studies.get` returns typed blocks (name/ref/config/complete via `server/modules/blocks.ts`). UI (`components/feature/builder/*`): `BuilderWorkspace` is the interactive three-zone body owning selection — StageTabs + work surface (`EditableStudyTitle` + subtitle + block list with Ready/Needs-setup badges + module-picker popover per `module-picker-popover.md`) + right panel flipping Details ↔ Configure (generic form: text/checkbox by value type, committed on blur). Reads via `api.studies.get` with the RSC study as `initialData`; mutations invalidate. Block `instanceId`s are ULIDs. **QA: 46 vitest** (+3 block-editing: add→configure→remove with badge flip, unknown-module reject, schema-invalid reject) + 4 Playwright e2e green; typecheck + production build clean. Authoring loop now buildable end to end; block editing test-covered (authenticated UI not sandbox-screenshot-able due to Clerk's client SDK). Next: Save-as-version dialog, then the full Hanna-loop e2e (`@clerk/testing`), then the Framework seed (item 9).
- **2026-06-02** — **docs(adr): ADR-0012 (block format + autosave semantics) accepted + Module/ModuleVersion data-model entry — the gate before block-editing code.** Verified the full MVP loop live in a real browser first. **ADR-0012** locks: block-instance format `{ blocks: [{ instanceId: ULID, source, key, version, config }] }` in `definition_snapshot`, `config` validated against `ModuleVersion.schema` via **Zod at the write boundary** (ADR-0001 schemas-first), `module_version_locks` as the derived `(source,key,version)` set; the **single `autosave` version is the mutable working tip** (updated in place), with `named`/`preregistered`/`published` immutable snapshots taken from it — framed as a **clarification of ADR-0002** (working directory vs. commits), preregistration snapshotting the autosave and ADR-0004 amendments still creating new immutable rows; **ULID** instanceIds; `config` is `Record<string,unknown>`/jsonb guarded app-side; **block-edit concurrency = last-write-wins in V1** (single-author per `build-a-study`), optimistic-counter in V1.5 (recorded trade-off); the `02-module-entities.md` entry lands same change; the migration **seeds `core/social-post@1.0.0` + `core/likert-7@1.0.0`**. `02-module-entities.md` defines Module + ModuleVersion (in-repo Zod = runtime schema; DB column stores JSON-Schema for record/query). Validator clean at 40. Owner approved Decision + Consequences; implementing straight through (tables+migration+seed → module picker → block CRUD/validate → Save dialog) with no further checkpoints. Also pushed the 3 stacked Builder commits to `origin/main`.
- **2026-06-02** — **feat(builder): tRPC HTTP client + React Query + editable title autosave.** Wired the client-mutation half of tRPC (deferred since the data-layer bootstrap): `lib/trpc/react.tsx` exports `createTRPCReact<AppRouter>()` as `api` plus a `TRPCReactProvider` (QueryClient + `httpBatchLink` to `/api/trpc`, no transformer — the server `initTRPC` has none and procedures return JSON-safe shapes incl. ISO date strings), mounted in the `(app)` layout so authenticated client components can mutate. First consumer: `EditableStudyTitle`, a client island in the Builder — click to edit, Enter/blur commits, Esc cancels — calling the new `studies.updateTitle` mutation (`workspaceProcedure`, tenant-scoped, NOT_FOUND outside the workspace; the title lives on `Experiment.title`, so renaming is a simple UPDATE with no version-model churn). RSC pages keep reading via the in-process caller; this adds the HTTP path. QA: **41 vitest** (added 2 `updateTitle` tests) + 4 Playwright e2e green; `npm run typecheck` + production build clean. Next (substantive remainder of Builder): block editing — a data-model/ADR decision on the block-instance shape + autosave-vs-immutability (ADR-0002 clarification), then `Module`/`ModuleVersion` tables (new migration) + seed, then the module picker (item 8) + block add/remove/validate, then the Save-as-version dialog; the Misinformation Research Framework seed (item 9) + the full Hanna-loop e2e (with `@clerk/testing`) close it out.
- **2026-06-02** — **feat(studies): Builder shell — three-zone Build stage (ADR-0011 MVP item 7, read-only shell, `build-stage-builder-mode.md` v0.5.3).** `studies.get` is a tenant-scoped `workspaceProcedure` query (NOT_FOUND for a study outside the active workspace) returning title/stage/versionNumber/ownerName/blockCount (blocks come from the current version's `definition_snapshot` — empty for blank studies; the formal block format stays deferred). Refactored the `(app)` layout to stop wrapping children in one work-surface card — each surface now composes the area right of the `LeftRail` itself (Studies wraps its content in a card; the Builder lays out its own three zones), matching the wireframes. The Build page renders a floating `StageTabs` pill spanning the center column (Build active, the other five stages inert), a work-surface card (static Plex Serif title — editing is next; `v{n} · {stage} · Edited {date}` subtitle; Builder/Whiteboard toggle with Whiteboard inert; a Blocks section showing the blank empty state with an inert `+ Add block` — module picker is item 8), and a right Details panel (`role=tablist`, Details active, others inert; "At a glance" Status/Owner/Tags). `notFound()` on an unknown/foreign id. Read-only shell — title editing, block add/drag, validation badges, and autosave arrive in the next unit, which pulls in the tRPC **HTTP client + React Query**. QA: **39 vitest** (added 2 `studies.get` tests: fetch a created study; tenant-scoped NOT_FOUND) + 4 Playwright e2e green; `npm run typecheck` + production build clean. The authenticated Builder isn't sandbox-screenshot-verifiable (Clerk client SDK); `studies.get` is test-covered. Next: the Builder editing unit.
- **2026-06-02** — **feat(studies): New study modal + `studies.create` (ADR-0011 MVP item 6, `new-study-modal.md`).** `studies.create` is a `workspaceProcedure` mutation that inserts the Experiment + its first version (v1, autosave, empty definition) and points `current_version_id` at it, all in one transaction; the modal (client) calls it through a thin `createStudyAction` server action, so we still don't need the tRPC HTTP client / React Query (deferred until a surface needs client-side queries or optimistic updates). UI: a `role=dialog aria-modal` modal (Esc + backdrop + close-icon dismiss) opened via a shared `NewStudyProvider` context mounted once in the `(app)` layout — so the TopBar's `+ New study` button **and** the Studies empty-state CTA drive the single modal. Three radio cards: **Blank** functional (`Create blank study` → draft → `/studies/[id]/build`); **Framework + Template** shown but disabled with a tooltip — exactly the wireframe's "no Frameworks available" edge case (the Framework entity + seeded Misinformation Research Framework are ADR-0011 item 9). Added a Build-stage placeholder at `/studies/[id]/build` (item 7) so create has a real landing, and study cards became clickable to it; removed the interim `/studies/new` page. QA: **37 vitest** — added 3 `studies.create` tests (scoped draft + v1 link, default title, unauth guard) over migrated PGlite (cleanup now nulls `current_version_id` before deleting, respecting the experiment↔version circular FK) — plus 4 Playwright e2e, all green; `npm run typecheck` + production build clean (9 routes). The authenticated modal interaction isn't sandbox-screenshot-verifiable (headless preview can't load Clerk's client SDK); the create path is test-covered, so eyeball in a real browser. Next: Builder mode (item 7) — the three-zone block surface, which finally pulls in the tRPC HTTP client + React Query for block edits/autosave.
- **2026-06-02** — **feat(studies): Studies destination — chrome + list + empty state (ADR-0011 MVP item 5).** `app/(app)/layout.tsx` is the authenticated app shell + canonical modular surface per `studies-destination.md`: a floating `TopBar` cap over a `[LeftRail · work-surface card]` row on parchment. It resolves the active workspace via tRPC (`workspace.active`) and redirects a signed-in-but-not-onboarded user to `/signup`. `components/chrome/top-bar.tsx`: workspace chip (switcher popover deferred, inert), `· Studies` breadcrumb, inert `⌘K` pill, live `+ New study` link (`aria-keyshortcuts` ⌘N), avatar initials. `components/chrome/left-rail.tsx` (client; `usePathname` for active state): the 7 IA destinations — Studies live, the rest shown but inert (deferred V1.5) so the rail reads complete without 404 links. `app/(app)/studies/page.tsx` (RSC via the in-process caller; reads `?filter`): Plex Serif `Studies` header + sub-nav filter tabs (links with `aria-current`; a full WAI-ARIA tablist with arrow-key roving is a noted follow-up) + either the study list (`components/feature/study-card.tsx` — stage badge token encoding per the wireframe; presentational until the Build stage exists) or the empty state (`Your first study is one click away.` + autofocused CTA), with a distinct filter-empty state. `+ New study` routes to a `/studies/new` placeholder so it isn't a dead button (the Framework/Template/Blank modal is the next item). `middleware.ts` switched from `auth.protect()` to an explicit redirect of unauthenticated users on protected routes to **our** `/signin` (`auth.protect()` targets Clerk's hosted Account Portal; we own the auth surface per ADR-0007); `ClerkProvider` gained `signInUrl`/`signUpUrl`; post-onboarding finalize + sign-in now land on `/studies`. QA: **34 vitest + 4 Playwright e2e** (new: unauthenticated `/studies` → our `/signin`) green; typecheck + production build clean. The authenticated empty-state/chrome visuals aren't screenshot-verifiable in this sandbox (the headless preview can't load Clerk's client SDK to seed a session) — verify in a real browser; the protection redirect + tenant-scoped list logic are test-covered. Next: New study modal (create-draft mutation → needs the tRPC HTTP client + React Query), then Builder mode + seeding the Misinformation Research Framework, then the full Hanna-loop e2e.
- **2026-06-02** — **feat(api): tRPC v11 bootstrap + workspace/studies routers (Studies destination data layer).** `server/trpc/trpc.ts` initializes tRPC over a `Context` whose identity comes through the `AuthAdapter` (`context.ts` — never Clerk directly); `protectedProcedure` requires an authed user **and** a local `user` row (attaches `dbUser`); `workspaceProcedure` chains on it to resolve + attach the **active workspace** (`server/workspace/active.ts`, owned-workspace-first) as the tenant boundary. Routers: `workspace.active` (chip + breadcrumb) and `studies.list` (tenant-scoped, joins the current `ExperimentVersion` for stage = draft/preregistered/published; sub-nav filters — archived split in SQL, the rest in-memory while V1 counts are small). Consumed two ways: an in-process **RSC caller** (`server/trpc/server.ts`, for read-only pages like Studies) and an **`/api/trpc`** fetch handler (for client mutations once New study lands). `zod` validates inputs (schemas-first per ADR-0001/0011). QA: `server/trpc/__tests__/studies.test.ts` — 7 tests via a directly-constructed caller over migrated PGlite (tenant scoping, empty workspace, archived include/exclude, `workspace.active`, and `UNAUTHORIZED`/`FORBIDDEN` guards); **34 vitest total green**, typecheck clean. New deps: `@trpc/server` v11, `zod`. Next: the Studies destination page UI + chrome + empty state per the wireframe.
- **2026-06-02** — **Commit 4 (feat(signup)): onboarding flow per the wireframe + e2e — ADR-0011 step 2 COMPLETE.** `05_app/app/(auth)/` holds custom `signup` / `signin` / `signup/verify` / `signup/sso-callback` pages on a centered ~480px parchment column per `signup-onboarding.md`, with **no Clerk prebuilt components** (ADR-0007) — built on Clerk client hooks (`useSignUp`/`useSignIn`/`useClerk`), the deliberate `(auth)`-surface lock-in exception. Identify step: email magic-link (`signUp.createEmailLinkFlow().startEmailLinkFlow` → `waitForEmailLinkVerification`, completed in the verify tab via the headless `handleEmailLinkVerification` hook — not the `<AuthenticateWithRedirectCallback>` component) OR Google OAuth (`authenticateWithRedirect` → sso-callback via `handleRedirectCallback`). Then profile (display name + theme via the reused `ThemeToggle`, now with WAI-ARIA arrow-key radio nav) → workspace name → finalize. `server/onboarding/finalize.ts` runs **one `postgres-js` interactive transaction** (user upsert on `external_id` + workspace with a unique slug + owner `member`), then writes `themeChoice`/`lastWorkspaceId`/`hasCompletedOnboarding` to Clerk `publicMetadata` through the adapter — no Clerk import in the action. `ThemeProvider` stayed **Clerk-free**: a thin `set-theme` server action persists durably (no-ops when signed out) while localStorage + `data-theme` give the instant switch; it also accepts a server-seeded `initialChoice`. `app/page.tsx` shows an authenticated welcome read via the adapter (the assertable post-onboard landing until `/studies` exists). **Vendor pin:** `@clerk/nextjs` locked to **v6.39.5** — v7.4.x re-routes hooks through `@clerk/react`'s experimental signals API (`SignUpFutureResource`, no `isLoaded`/`setActive`), which diverges from the documented classic API; v6 keeps the classic `{ isLoaded, signUp, setActive }` + `createEmailLinkFlow`/`authenticateWithRedirect` the verified docs describe (the server adapter is unaffected — still async `auth()`/`clerkClient()`). The exact client method signatures were checked against the installed `@clerk/shared` `.d.ts` before coding, not guessed. **QA** per `qa-and-testing.md` + ADR-0011: **27 vitest tests** (12 adapter + 6 finalize-transaction integration via pglite + 9 migration) all green; **3 Playwright e2e** (signup identify step, sign-in surface, unauthenticated home) green against a production server on a dedicated port; `npm run typecheck` + `npm run build` clean; a grep confirms the only `@clerk` imports outside `server/adapters/` are the three inventoried exceptions. The full browser-driven `signup→onboard→land` flow (needs a seeded Clerk session + the Studies destination) is the dedicated Hanna-loop e2e move next; its finalize logic is already covered green by the integration test. Next: Playwright full Hanna loop, then the Studies destination (tRPC `studies.list` + three-zone layout).
- **2026-06-02** — **Commit 3 (feat(auth)): Clerk AuthAdapter + provider + route protection.** `05_app/server/adapters/auth.clerk.ts` implements the `AuthAdapter` against **Clerk v7** and is the only file importing `@clerk/nextjs/server`: `getCurrentUser` maps a Clerk user to our `AuthUser` (primary email lowercased, `displayName` from `fullName`, `avatarUrl`, `hasCompletedOnboarding` from `publicMetadata`) and is null-safe (never throws); `requireCurrentUser` throws a typed `UnauthorizedError`; `getCurrentSession` derives `createdAt`/`expiresAt` from `sessionClaims` `iat`/`exp` (no extra Backend API call); `signOut` revokes the active session via `clerkClient`; `getUserMetadata`/`setUserMetadata` read+write the narrow `publicMetadata` bag (Clerk shallow-merges, so partial patches are safe). `auth.ts` retired the throwing Proxy → `auth = clerkAuthAdapter` (the one-line vendor-swap point) and `AuthUserMetadata` gained `hasCompletedOnboarding` so onboarding finalize (commit 4) writes it through the adapter, never importing Clerk in feature code. `app/layout.tsx` wrapped in `<ClerkProvider>`; `middleware.ts` uses `clerkMiddleware` + `createRouteMatcher` to protect the future `/studies`, `/library`, `/frameworks`, `/settings` paths (the `(auth)` pages stay public; route-group folders aren't part of the URL, so the matcher targets real paths). `04_architecture/lock-in-inventory.md`'s Clerk row now records **three deliberate exceptions**, all removed on the Clerk→Better Auth migration: (1) `<ClerkProvider>` at the layout root, (2) `clerkMiddleware` at the project root, (3) the `(auth)` pages' client hooks (`useSignUp`/`useSignIn`/`useClerk`) — building custom auth UI (ADR-0007 bans Clerk's prebuilt UI components) requires the client SDK, and the `AuthAdapter` only ever covered server identity. The adapter README's current-adapters table now points `AuthAdapter` at `auth.clerk.ts`. QA: 12 adapter unit tests (happy + auth-fail + field mapping + metadata + signOut, Clerk fully mocked) plus the 9 migration tests = **21 green**; `npm run typecheck` clean; a `grep` confirms no `@clerk` imports leak into feature code (only the two framework exceptions + the adapter folder). Next: commit 4 — the `/signup` onboarding flow per the wireframe (custom UI), the finalize transaction, `ThemeProvider` Clerk-sync, and the signup-slice Playwright e2e.
- **2026-06-02** — **Commit 2 (feat(db)): Drizzle bootstrap + first migration.** `05_app/server/db/schema.ts` is the single source of truth for the database shape — `user` / `workspace` / `member` (data-model 01) + `experiment` / `experiment_version` (data-model 00), five pgEnums (`member_role`, `member_status`, `forkable_by`, `experiment_version_kind`, `amendment_classification`), and every CHECK the data model specifies: experiment fork half-null consistency, `experiment_version` name-required-unless-autosave, the ADR-0004 amendment pair (`supersedes_version_id` + non-empty `change_summary` both set or both null), and `member` status/user consistency; plus unique indexes for one membership per `(workspace, user)` and monotonic `(experiment, version_number)`. The `experiment ↔ experiment_version` circular FK is resolved with a nullable `current_version_id` + post-create `ALTER TABLE` (drizzle-kit handled it). Driver is **postgres-js** with `prepare:false` (`server/db/client.ts`), **not** `drizzle-orm/neon-http`, because onboarding finalize (commit 4) needs an interactive transaction the HTTP driver can't do. `drizzle.config.ts` loads `.env.local` (drizzle-kit, unlike Next, doesn't auto-load it). The first migration `0000_quiet_vampiro.sql` was generated by drizzle-kit and **applied to the Neon dev DB** (one harmless `NOTICE`: a 63-char FK identifier truncated by Postgres — deterministic on both generate and apply, so no drift). QA per `qa-and-testing.md`: a hermetic **pglite** migration test (`server/db/__tests__/migration.test.ts`) — 9 tests green covering forward apply (tables/enums/named CHECKs exist), four constraint-rejection/acceptance cases, and a teardown+reapply idempotency check standing in for "rollback" (drizzle-kit emits no down-migrations; documented in the test). `npm run typecheck` clean. New dev deps: `drizzle-kit`, `dotenv`, `@electric-sql/pglite`, `vitest`; new scripts `db:generate` / `db:migrate` / `test`. Next: commit 3 — Clerk `AuthAdapter` + `ClerkProvider` + `middleware.ts` + the three lock-in exceptions + adapter unit tests.
- **2026-06-01** — **GitHub remote + push done; ADR-0011 step 2 started with commit 1 (docs gate).** Project owner completed the one-time setup checklist (created the GitHub repo, pasted Clerk publishable + secret + Neon `DATABASE_URL` into `05_app/.env.local`, confirmed the scaffold). This session added the `origin` remote over **HTTPS** (no SSH keys present at `~/.ssh/id_*`; `gh` is authenticated for account `lowcydizajnu`) and pushed all three existing commits (`326f9ee` baseline + `27dea60` tooling + `f9ff5d8` status); `main` now tracks `origin/main`. Also bootstrapped the manifest tooling on this fresh machine: installed PyYAML per the manifest README so `validate.py` runs (use `python3`, not `python`). Then the work was scoped, planned, and approved as **4 small honest commits** (data-model gate → Drizzle → Clerk → signup), with the signup-slice Playwright e2e landing in commit 4. **Commit 1 cleared the first phase-gate blocker before any app code:** `00-core-entities.md` had explicitly deferred the auth/tenancy entities, so writing the Drizzle migration would have invented their shape. New `04_architecture/data-model/01-auth-tenancy-entities.md` defines **User, Workspace, Member** and locks the load-bearing decision **Workspace IS the tenant** — the sketch's `tenant_id` resolves to `workspace.id`, there is no separate Tenant entity, and Clerk is identity-only with membership living in our Drizzle `member` table (NOT Clerk Organizations) per ADR-0007, so an auth-vendor migration only re-points `user.external_id` and never touches the authorization graph. `hasCompletedOnboarding` + `themeChoice` + `lastWorkspaceId` deliberately live in Clerk `publicMetadata`, not user columns (avoids a two-writer consistency problem); `member.role` (owner/admin/editor/viewer) + `status` (active/invited) are present so the later invite/Team work is an additive migration, not an enum alter. New `02_product/feature-specs/signup-and-onboard.md` is the feature-spec gate artifact (ADR-0011 step (c)): Problem / Goals / Non-goals / 9 testable functional requirements / NFRs (adapter discipline, a11y `role=radiogroup` + `aria-live`, determinism, atomic transaction) / QA plan, linking the `signup-and-onboard` flow + `signup-onboarding` wireframe + ADR-0007/0011 + the new data model. The four commits' Clerk + Drizzle APIs were pre-verified against live Clerk v7 + Drizzle docs (async `auth()`/`clerkClient()`, `clerkMiddleware`, `email_link` + `waitForEmailLinkVerification`, `oauth_google` `authenticateWithRedirect`, `publicMetadata` server-write, `postgres-js` for interactive transactions) to avoid hallucinated vendor APIs — the project's flagged highest-severity failure. Validator clean at 38 instances (+1 data-model entry; the folderless feature-spec sits beside the feature, not indexed). Next: commit 2 — Drizzle bootstrap + first migration + hermetic `pglite` migration test.
- **2026-05-29** — **Repo initialized + tooling-discipline rules added.** Git repo at project root, branch `main`. Baseline commit `326f9ee` captures all Phase 0-5 work to date (120 files). Root `.gitignore` covers node_modules, .env, OS noise, Dropbox conflict files, Python caches. Scaffold visually verified by project owner. New `HANDOFF.md` at project root: a universal session-start anchor that says "read CLAUDE.md then STATUS.md, that's the whole system" plus a table of where to switch tools (Cowork tab for design/strategy; Code tab for code writing; the user's machine for running the app), starter messages for each tab, current waypoint, project-owner setup checklist (GitHub remote, Clerk account, Neon DB), what "lost context" symptoms look like + the fix for each, and a file-map for "if you need X, read Y." Two new behavior rules added to CLAUDE.md: (1) **Implement design from the spec, never invent** — work order is wireframe spec → tokens.md → reference render at app/page.tsx → brief v0.6; tokens by name never raw hex; missing tokens added to tokens.md + tokens.css with Light + Dark in the same change; vendor SDK imports live only in 05_app/server/adapters/<concern>.<vendor>.ts per ADR-0007 + lock-in inventory. (2) **Commit on the user's behalf in small honest units** — git commit as part of finishing a unit, not a separate step; imperative <72 char subject; body links spec/ADR/STATUS; Co-Authored-By trailer; never push without confirmation; never rewrite shared history. Second commit landed: `feat(tooling): handoff doc + design-implementation + commit-on-behalf rules`. Validator clean at 37 instances. Next: project owner creates GitHub repo + adds remote (commands below); then switches to Code tab for Clerk + Drizzle + signup commit per ADR-0011 step 2.
- **2026-05-29** — **Adapter discipline + ADR-0007 amendment landed before Clerk wires.** Project owner asked for fresh auth-provider comparison; chose to stay with Clerk for V1 with direction "be prepared to migrate — maybe it won't happen but just not to be blocked and locked." Mechanical preparation: `05_app/server/adapters/auth.ts` defines the typed `AuthAdapter` interface (`getCurrentUser` / `requireCurrentUser` / `getCurrentSession` / `signOut` / `getUserMetadata` / `setUserMetadata`); narrow `AuthUserMetadata` shape exposes `themeChoice` + `lastWorkspaceId` so `ThemeProvider` swaps localStorage-only → Clerk-synced without touching component code; default `auth` export is a `Proxy` that throws on first call until `auth.clerk.ts` lands — guarantees no silent misuse before the implementation exists. Workspace membership lives in our Drizzle `member` table, NOT Clerk's Organizations primitive — adapter exposes identity only. `05_app/server/adapters/README.md` documents the one rule: no `@clerk/nextjs` (or `liveblocks` or `inngest`) imports outside this folder. Strategic preparation: `04_architecture/lock-in-inventory.md` tracks Clerk + Vercel + Liveblocks + Inngest + OSF + AI providers — per-vendor what-we-use-it-for, what's behind the adapter, deliberate exceptions, migration target, cost-ceiling trigger. PR review consults the file when adding any vendor SDK import. ADR-0007 amended: Clerk migration target shifts from Auth.js → **Better Auth** (TypeScript-first, organizations primitive built in, first-class Drizzle integration; Auth.js + Drizzle has known sharp edges; Lucia Auth sunset). Cost ceilings ($200/mo plan, $500/mo execute) and migration order (Clerk first) unchanged. Phase pipeline: Build 2/2 (scaffold + adapter prep). Validator clean at 37. Next commit: install `@clerk/nextjs`, implement `auth.clerk.ts` against the interface, middleware protecting `(app)/*`, `/signup` route per signup-onboarding wireframe, first Drizzle migration.
- **2026-05-29** — **Phase 5 (Build) started.** First commit in `05_app/`. Stack per ADR-0011: Next.js 15 + React 19 + TypeScript + Tailwind v4 (CSS-driven config, no `tailwind.config.ts`) + IBM Plex Sans/Serif/Mono via `next/font/google`. Token system materialized as runtime CSS variables — `styles/tokens.css` has every Light value on `:root` and every Dark value under `[data-theme="dark"]`, mapped 1:1 from `03_design/design-system/tokens.md`. Tailwind v4 picks them up via `@theme inline` in `app/globals.css`. Dark-mode variant uses `@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *))` driven by the `data-theme` attribute, not Tailwind's `.dark` class — so user toggle and `prefers-color-scheme` both flow through one mechanism. `ThemeProvider` (`components/theme-provider.tsx`) tracks user choice (light / dark / system), resolves it to a rendered state, persists to localStorage, and listens for OS theme changes when `system` is active; Clerk metadata sync wires in the auth iteration. `ThemeToggle` (`components/theme-toggle.tsx`) is the three-card picker matching the signup-onboarding and account-settings wireframes, including mini-mockup swatches for each option. Verification page at `/` exercises the full design language: parchment background, Plex Serif `Build studies. Document everything.` headline, theme picker that immediately flips the surface, palette swatches showing the polarized two-stop ramps for primary/accent/success/danger, and a demo block-validation row with a vibrant emerald `schema valid` chip and a vibrant red `missing field` chip — same visual treatment as the Builder wireframe. `lib/utils.ts` provides the `cn` helper; `components.json` configures shadcn-cli for the next iteration; `.env.example` covers Clerk/DB/R2/Liveblocks/Inngest keys; `.gitignore` set up. README documents install + run + Dropbox warning (xattr to ignore `node_modules`). Phase pipeline: Build 1/1 (this commit). Validator clean at 37 (no manifest artifacts added — code lives outside it). Next iteration: Clerk + Drizzle + tRPC bootstrap → failing Playwright e2e for Hanna's full MVP loop → ship features.
- **2026-05-28** — **v0.6 milestone: Phase 3 (Design) complete enough for Build.** Speed-up turn driven by project owner request "all wireframes + tokens + start MVP." Triage applied (per pushback recommendation): lock-not-iterate parchment at #F7F2E8; MVP-required wireframe scope only (~5 more, post-MVP deferred); plan-now-scaffold-next-session build cadence. Shipped in this turn: (1) **brief → v0.6** — locked, no further iteration; (2) **+1 JTBD** `get-set-up.md` covering auth/onboard/account; (3) **+4 user flows** signup-and-onboard, manage-account-settings, browse-library, browse-frameworks; (4) **+5 wireframe specs** library-browse, frameworks-browse, account-settings, signup-onboarding, module-picker-popover — total wireframe coverage now 9 specs across MVP-required surfaces; (5) **composite MVP visualizer** shown in chat (Signup centered-column + Library browse + Account Settings Appearance tab on warm parchment); (6) **tokens materialized** at `03_design/design-system/tokens.md` — Light + Dark values per token, canonical source for code, includes Themeability paraphrase and CSS-variable contract; (7) **ADR-0011 (scaffold strategy) accepted** — Next.js 15 + tRPC v11 + Drizzle + Postgres + Clerk + Tailwind + shadcn/ui, full directory layout under 05_app/, MVP slice explicitly defined (auth → Studies → New study from Framework → Builder 2 blocks → Save as named version → return tomorrow find draft), deferred-from-MVP list explicit (Whiteboard, Replications, Activity, Participants, Team, Preregister/Share/Run/Results, OSF push, AI, recruitment). Phase 5 (Build) ready to start. Validator clean (37 instances, +12 since v0.5.3 lock). Project owner answered three recommended options on AskUserQuestion (MVP-only wireframes, Hanna-end-to-end MVP, plan-now-scaffold-next-session) before triage executed.
- **2026-05-28** — **Design-language brief → v0.5.3.** Stage-tabs placement re-locked from Option A to **Option B** after the project owner saw both visualized side-by-side in chat. Option A (v0.5.2 — Typeform pattern, stage tabs in top-bar center slot) was familiar but reintroduced a dense full-width band that pulled against the brief's modular-floating-panels spirit. Option B is structurally closer to Miro / FigJam: the page is a board of floating modules and each module owns its own controls; the stage-tabs pill floats over the work surface it controls. Top bar reverts to slim form (workspace chip + breadcrumb on left; ⌘K + Save when applicable + user avatar on right — no stage tabs). Stage-tabs pill spans only the center column's width (not the full page) and sits on top of the work surface card with a small internal gutter. Left rail and right context panel cards extend full height of the center column. The reading is now: "three modules on a parchment board — workspace nav on the left, the study being built in the middle (mode picker on top + work surface beneath), context on the right — with a slim top hat carrying global chrome above all three." Brief Layout system + Top-bar navigation sections rewritten; `03_design/wireframes/build-stage-builder-mode.md` Layout + Content inventory + Stage-tab-click interaction revised to match. Validator clean (25 instances, no count change — layout-only revision).
- **2026-05-28** — **Design-language brief → v0.5.2.** Layout-hierarchy correction surfaced by the first wireframe pass. Project owner observed the disconnect between top bar / sidebar and the separate full-width stage-tabs strip: the stage strip stretched above the sidebar in v0.5.1's mockup, but stage tabs are **study-scoped** while the sidebar is **workspace-global**. Adjacent full-width rows implied the same hierarchy level, which is wrong. **Fix (Option A — Typeform pattern):** stage tabs move into the **top bar's center slot** between breadcrumb (left) and Save (right). Top bar grows in height. Sidebar starts directly below the top bar with no intervening full-width strip. The scope hierarchy becomes spatially literal — workspace-global chrome (sidebar) is one tier; study-scoped chrome (stage tabs) is nested inside the top bar, not stretched across the sidebar. Top-bar content is now determined by what the breadcrumb points at: on a destination (Studies, Library, etc.) the center slot is empty and the Save button hides; on a study object the center slot carries the six stage tabs (`Build · Preview · Share · Preregister · Run · Results`) and Save appears on the right. Updates: brief Layout system + Top-bar navigation sections rewritten; `03_design/wireframes/build-stage-builder-mode.md` Layout + Content inventory + Interactions revised; re-rendered the Builder visualizer Light + Dark side-by-side with the corrected pattern in chat. Validator clean (25 instances; no count change since correction was layout-only).
- **2026-05-28** — **First wireframe pass landed** (Phase 3) + **first JTBD** (`02_product/jobs-to-be-done/build-a-study.md`) + **first user flow** (`02_product/user-flows/hanna-build-a-study.md`). Phase-gate sequence honored per CLAUDE.md (no wireframe without a user flow; no user flow without a JTBD). Hanna's "build a study" flow covers happy path (Framework / Template / Blank → Builder → Save), three decision points (start-from-what, module-fits, save-semantics), five failure modes (network drop, validation-failing-save, framework-updated, asset-link-dead, mid-edit-save), Mermaid diagram, and five open questions. JTBD `Build a study` anchored on Hanna validated against Maya's review role, with forces, alternatives the researcher currently "hires" (Qualtrics+Doc, PsychoPy+GitHub, etc.), and measurable outcomes. Four wireframe specs drafted: `studies-destination` (Hanna's home base — list + sub-nav + new-study entry), `new-study-modal` (Framework/Template/Blank picker), `build-stage-builder-mode` (the meat — three-zone modular surface, stage tabs, study title in Plex Serif display, block cards with vibrant valid/missing badges, right context panel tabs), `save-as-version-dialog` (three save semantics matching ADR-0002 version kinds). Each spec covers Purpose, Layout, Content inventory, States (Default / Loading / Empty / Partial / Error / Offline / Read-only), Interactions, Edge cases, Accessibility notes, Open questions. **Showed two visualizers in chat side-by-side Light + Dark**: (1) Studies destination — top bar floating cap, left rail card, center work surface card with sub-nav and three study cards (draft / preregistered / published with vibrant tinted badges), all on warm `surface.page #F7F2E8` (Light) and warm `#161514` (Dark); (2) Build stage Builder mode — full chrome with stage tabs (Build active), study title in Plex Serif display, two block cards (one schema-valid emerald, one missing-field red), right context panel Details tab with Plex Serif "At a glance" head. Dark variant uses warm-dark surfaces (#161514 / #1B1A18 / #1F1D1B) with off-white text #F5F1E8 and the same vibrant functional colors. Validator clean (25 instances, +6 since v0.5.1 baseline: +1 JTBD, +1 user flow, +4 wireframe specs).
- **2026-05-28** — Design-language brief → **v0.5.1**. Three refinements after v0.5 read as "too creamy" + project-owner asked for theming + light/dark V1 commitment. (a) **Parchment paled**: `surface.page #F4EFE6 → #F7F2E8` and `surface.panel #FBF7EE → #FCFAF3`. Final tone explicitly held open to confirm alongside wireframes — project owner wants to "see bigger picture before deciding final." (b) **Dark mode upgraded from post-V1 to V1 ship**: both Light and Dark themes available, user-toggleable, persisted per user; System (auto-follow OS) is the default for new users. Every neutral and border token now has Light + Dark values; dark-mode surfaces are warm-dark (#161514 page, #1F1D1B canvas, #1B1A18 panel) not cool slate — preserves editorial tone. (c) **Themeability made structural**: new Themeability section in the brief explaining tokens as CSS variables, themes as bundles of token values, Light + Dark for users, internal exploration themes (live in `03_design/design-system/themes/`, dev-mode switch only, never reach production) so we can audit decisions against alternative visual directions. Hard rules: no raw hex in components, both Light + Dark values added in same change, both modes contrast-checked before handoff. ADR-0010 (theming system) flagged but deferred to Build — straightforward CSS-variables + ThemeProvider implementation. v0.5 modular floating panels + Plex Serif in more roles + v0.4 functional vibrancy all preserved. Validator clean (24 types, 19 instances).
- **2026-05-28** — Design-language brief → **v0.5**. Project owner referenced a Typeform Builder screenshot and gave directional feedback: "color intensity is fine but it's Mural not Miro — want to be more Miro; use beige as the app background; more editorial / typography-sophisticated since we have a fantastic font; modern apps have modules/surfaces, this is stitched together." Three corrections: (a) **page background becomes warm parchment** — new `color.surface.page #F4EFE6` retiring v0.4's cool `surface.sunken #F4F6FA`; the whole product sits on a warm, editorial page; (b) **modular floating panels** — left rail / center work surface / right context panel each render as their own card with `border.subtle #E6DFD2` outline, `radius.lg` rounded corners, warm-cream `color.surface.panel #FBF7EE` for the side rails vs white `surface.canvas #FFFFFF` for the work surface, 12-16px gutters between cards revealing the warm page background (the Typeform / Linear / Miro pattern); (c) **Plex Serif takes more weight** — study titles in display sizes (32-40px Serif 500), section heads inside panels in Serif 500 (was Sans), stage indicators where editorial gravity helps. Functional palette vibrancy from v0.4 preserved (`success #22C55E`, `danger #EF4444`, `warning #F59E0B`); `.subtle` washes nudged warm so they don't sit on the parchment page as cool islands. Heavy `border.heavy #1A1F2C` now reserved (was the structural workhorse); the modular panels carry the structural work via the page-bg gap. Showed v0.5 visualizer widget in chat: warm parchment frame, three floating cards with their own warm cream/white backgrounds and rounded corners, prominent Plex Serif study title, Preregister stage tab as a floating "pill" card, validation badges in vibrant emerald/red. Validator clean (24 types, 19 instances).
- **2026-05-28** — Design-language brief → **v0.4**; IA → **v0.3** with five new conceptual picks locked. **Palette v0.4 corrections** (driven by project-owner feedback on v0.3 visualizer — "make green/red more vibrant; pale or full nothing in the middle; avoid AI slop like vertical right-side edges"): functional palette pushed to true vibrancy (`success #22C55E`, `danger #EF4444`, `warning #F59E0B`); shade ramps polarized to two stops only (very pale `.subtle` + full token), mid-tone `.hover` hues removed in favor of opacity overlay; zone separation moves from `border.heavy` vertical rules to background tint shift (new `color.surface.sunken #F4F6FA` for rail + right panel vs `color.surface.canvas #FFFFFF` for work surface) — heavy borders restricted to horizontal structural breaks only. **IA v0.3 picks (five new):** (a) forks-as-relationship surface in three places (forker's Studies with `Replicating ...` subtitle + new `Replicating` sub-nav, parent's right-panel `Replications` tab two-directional, Activity stream) — no top-level Forks destination; (b) Templates revoked as Frameworks alias — Frameworks = curated systems (schema + modules + measurement + reporting), Templates = paste-ready starter studies, Templates moves to `Library · Templates` sub-section; (c) Activity `Yours / Follows` split with five follow targets (tag, author, Framework, study, saved search) — `+ Follow` affordance on every relevant chip / byline / details tab; (d) Participants destination structured as five sub-views (Panels / Open recruitment / Compensation / Quality / Connections) — aggregated only, never PII, explicitly not a CRM; (e) four template share scopes (Public-replicable default per ADR-0002 / Workspace / Invite link / Submit to Framework) via a single `Share` panel on the saved version. Showed v0.4 visualizer widget: three-zone mockup with zone separation via background tint (no vertical heavy edge), heavy horizontal break under top bar only, Preregister stage tab active, true-vibrant validation badges (emerald success + red danger), Replications tab visible in right panel, Participants five-view inset. Validator clean (24 types, 19 instances).
- **2026-05-28** — Design-language brief revised to **v0.3**; IA advanced to **v0.2** with picks locked. **Palette revision** (driven by project-owner feedback that v0.2 "looks a bit stale, like nothing new and fresh — wants more vibrant and bold, more contrasts, dark lines to divide sections"): primary changed from `#0F2A47` to `#1747C9` (electric royal blue) for action surfaces, secondary accent added `#0EA5E9` (cyan) for highlights and energy, deep navy preserved as `color.ink.deep` for editorial-feel headings, three-tier border weights introduced for the dark-divider move (`border.heavy #1A1F2C` 1.5-2px between zones, `border.medium #94A0B5` for sub-sections, `border.subtle #DDE1E8` within elements), functional palette pushed up in saturation (`success #1F8A52` / `warning #D97706` / `danger #DC2626` / `info #0EA5E9`). **IA picks (v0.2):** Linear-style workspace switcher; global search with scope dropdown (Everything / This workspace / Studies / Library / Frameworks / Members); comments on both right-panel tab AND bottom drawer (to compare in real use); no notification bell (Activity destination handles it); stage tabs now `Build · Preview · Share · Preregister · Run · Results` — Preregister is its own stage between Share (peer review) and Run (commitment). Framework naming TBD with recommendation to keep "Frameworks" canonical and alias "Templates" in search. IA file renamed from `v0.1-information-architecture.md` to `information-architecture.md`. Showed v0.3 visualizer widget in chat: three-zone mockup with dark zone dividers, schema-validation callout in accent cyan, Preregister stage tab active, palette swatches, border-weight examples.
- **2026-05-28** — Design-language brief advanced to v0.2; first IA pass written. Brief locks IBM Plex (Serif/Sans/Mono — using all three intentionally) + synthesis palette (deep navy `#0F2A47` primary + neutral structure + muted functional palette + unified-saturation category chips). Full token table for primary, neutrals, success/warning/danger/info specified. IA v0.1 at `03_design/ia/v0.1-information-architecture.md`: top-level destinations (Studies / Library / Frameworks / Participants / Activity / Team / Settings) ordered by Hanna+Maya frequency; flat Studies (no Project nesting, tags do grouping); breadcrumb pattern `Workspace · Destination · Object`; stage tabs `Build · Preview · Share · Run · Results` orthogonal to breadcrumb; right-panel tab system specified per object type (Study / Module Instance / Framework / Asset / Module catalogue); Builder ↔ Whiteboard toggle in study top-bar with `⌘\\` keyboard shortcut; responsive collapse pattern. Validator clean (19 instances). Showed visualizer widget in chat — three-zone mockup + palette swatches + type sampler with the locked picks applied.
- **2026-05-28** — Phase 3 (Design) started. Set up `03_design/inspiration/` with `README.md` (intake convention) + `2026-05-28-first-batch.md` (six annotated references curated by project owner via Mobbin: Maze, Notion, TheyDo, Typeform Builder, Typeform Logic, Typeform Modal). Identified the three-zone layout pattern in 4 of 6 references and the builder + whiteboard duality (Typeform Build/Logic show same data, two views — direct map to ADR-0001's modular composition). Wrote `03_design/design-language-brief.md` v0.1 synthesizing inspiration + project-owner principles: tone (serious but fresh), neutral palette with one accent (candidate deep teal/blue), Inter as typeface candidate, 4px spacing grid with semantic layers, three-zone canonical layout with right context panel (selected-item drawer + live preview), customization limited to zone-collapse and tab-pin for V1, responsive breakpoints sketched. Open questions surfaced: accent color pick, typeface pick, dark-mode timing, participant-facing visual language as separate brief. Validator clean (18 instances).
- **2026-05-28** — **ADR-0009 accepted** (`04_architecture/adrs/0009-supply-side-strategy.md`). Three-layer hybrid strategy for the biggest non-technical risk: Layer 1 default virtue (architectural posture — already structurally enabled by ADRs 0001-0007 via schemas-first, immutable versions, asset freeze, preregistration amendments, OSF push; ongoing discipline in product/UX decisions); Layer 2 demand-first V1 GTM (consortia, replicators, open-science labs; explicitly NOT reluctant-author segments); Layer 3 mandate leverage V2+ partnerships (selected journals + funders; gated by V1 product evidence). Explicitly accepts that the back catalogue of un-runnable studies is lost — flow > stock. Strategic ADR; more posture commitments than code commitments. Validator clean (16 instances).
- **2026-05-28** — **ADR-0007 accepted** (`04_architecture/adrs/0007-path-a-vs-b.md`). Path A (Vercel + Clerk + Liveblocks + Inngest) with adapter discipline. Four interfaces: `HostingShape`, `AuthAdapter`, `RealtimeAdapter`, `BackgroundJobAdapter`. No vendor-specific types in app code. No Clerk pre-built UI in production. Lock-in inventory at `04_architecture/lock-in-inventory.md` (to be created at Build start) tracks deliberate exceptions; PR review enforces the discipline. Cost-ceiling triggers: $200/mo plan, $500/mo execute, per single managed service. Per-piece migration order: Clerk first (per-MAU pricing makes it worst at scale), then Liveblocks, then Inngest, hosting last. The adapter philosophy is now consistent across infrastructure (this ADR), registries (ADR-0005), AI providers (ADR-0006), and modules (ADR-0001). Validator clean (15 instances).
- **2026-05-28** — **ADR-0006 accepted** (`04_architecture/adrs/0006-ai-plugin-architecture.md`). Task-based AI architecture per product-brief decision §2 (V1 architecture-ready, V2+ feature-bearing). Option B: Tasks are first-class typed operations with input/output schemas + prompt templates; AIProviderAdapter interface mirrors RegistryAdapter from ADR-0005; schemas-first validation enforced via ADR-0001's existing module schema infrastructure; per-tenant routing with sensitivity tags (`metadata_only` / `experiment_definition` / `participant_data` / `pii`); append-only `ai_invocation` audit log; per-tenant cost metering (`TenantAIMeter`); caching for deterministic tasks; human-in-the-loop UX default. V1 ships interface + stub adapter + gateway + audit + metering; zero AI features. Four new data-model entities sketched (AIProvider, AIProviderConnection, AIInvocation, TenantAIMeter). Validator clean (14 instances).
- **2026-05-28** — **ADR-0005 accepted** (`04_architecture/adrs/0005-osf-integration.md`). Push-to-OSF on preregistration via OAuth + Inngest async push. Sub-option B.3: JSON snapshot + rendered PDF + OSF template-field mapping + materials. Per-user OAuth (researchers own their OSF identity), default-on per-registration opt-out, async with retry, failed pushes flag the version without blocking preregistration. **Registry-agnostic adapter interface** so future registries (AsPredicted, ClinicalTrials.gov, PsyArXiv) plug in via the same shape. Amendments push as new OSF registrations referencing the prior DOI. Sketched three new data-model entities (Registry, RegistryConnection, RegistryPush) for a future entry. Validator clean (13 instances).
- **2026-05-28** — Persona work parked at synthetic-pilot validation; real interviews deferred due to access constraints, revisit triggers explicit. Drafted three new persona files based on the synthetic pilot archetypes: `postdoc-operator.md` (HK — daily tool operator the PI delegates to), `multi-site-coordinator.md` (MS — consortium operations, highest WTP signal), `burned-replicator.md` (SM — reproducibility-driven reformer, flagged as least representative / convert-by-injury). All three at `Status: in review`, `Evidence basis: SYNTHETIC pilot only`, with explicit revisit triggers. Updated `product-brief.md` with three new sections (§8 segment-dependent wedge value, §9 supply-side risk, §10 inverted trust signals) all citing synthetic-pilot evidence and naming the need for real-user validation. Validator clean (12 instances).
- **2026-05-28** — Integrated synthetic-pilot interview pass uploaded by project owner. Preserved source at `01_research/user-research/notes/2026-05-28-synthetic-pilot-interviews.md` (with raw extract alongside). Wrote new insight `01_research/insights/persona-segmentation-and-strategic-risks.md` capturing three strategic findings: (1) wedge value is segment-dependent — provenance/reproducibility beats authoring richness for the multi-site and burned-replicator segments; (2) supply-side incentive problem (why would authors share?) is the biggest non-technical risk and surfaces independently from two of three personas; (3) trust signals (incl. "modern UX") work inversely across segments — polish reads as credibility for one segment, suspicion for another. Cross-referenced from the literature insight and from Maya's persona. Three new persona archetypes implied (solo postdoc, multi-site coordinator, burned replicator) — queued. **Critical framing preserved throughout: the pilot is SYNTHETIC, not field data — every finding is a hypothesis to validate with real interviews.** Validator clean (9 instances).
- **2026-05-28** — **ADR-0004 accepted** (`04_architecture/adrs/0004-preregistration-amendments.md`). Formalizes how legitimate corrections to immutable preregistered versions work: a new `kind: preregistered` ExperimentVersion with `supersedes_version_id` (FK) + required non-empty `change_summary` + optional `amendment_classification`. Both versions remain public, immutable, queryable. UI shows lineage in both directions. Matches OSF / ClinicalTrials.gov / AsPredicted convention. Three new fields added to ExperimentVersion in `00-core-entities.md`; DB CHECK + app-level constraint specified. ADR queue renumbered: 0005 = OSF integration, 0006 = AI plug-in, 0007 = Path A vs B, 0008 = plugin model. Validator clean (8 instances).
- **2026-05-28** — **ADR-0003 accepted** (`04_architecture/adrs/0003-asset-storage.md`). Hybrid asset storage: researcher chooses per-asset between internal R2 upload and external link; auto-freeze on preregistration/publication (per-asset opt-out); failed freezes flag the version with `replication_risk: link_only`; content-hash dedup for internal assets; per-tenant storage metering architected (pricing model deferred). Sketched Asset and TenantStorageMeter entities for future data-model entry. Validator clean (7 instances).
- **2026-05-28** — Sketched core data-model entities in `04_architecture/data-model/00-core-entities.md`. Mermaid ER diagrams + per-entity fields, invariants, relationships for Module, ModuleVersion, Theme, Experiment, ExperimentVersion. Fork is not a separate entity — it's an Experiment with FKs (explained in doc). Framework + FrameworkVersion sketched briefly as parallel to Experiment. Resolved 5 ADR contradictions: mutable themes vs. immutable versions (theme_snapshot embed); module locks JSON vs. join table (JSON for V1); fork = copy or reference (both, at different layers); cross-tenant forks (allowed, gated by forkable_by); deprecated modules (informational, not refused). Surfaced 5 open questions for design phase. Validator clean at 6 instances.
- **2026-05-28** — Triangulated PI persona via literature. Wrote `01_research/insights/researcher-tooling-pain-points.md` synthesizing 20 Consensus searches (~300 papers received, 29 cited). Confirmed: tool ecosystem is fragmented; open-science adoption uneven (20–25% preregistration); cross-institutional collaboration tools exist but are separate from authoring tools; misinformation research has emerging dedicated tools (validates the thematic-spaces architecture). New findings: Prolific has displaced MTurk on data quality; "GitHub for research" demand is real but supply is fragmented (validates the wedge); sustainability is itself a credibility signal. PI persona status lifted from `draft` to `in review`; Grounding insights linked. Interviews still needed to upgrade to `validated`. Insight artifact type added to manifest; 5 instances now in workspace, validator clean.
- **2026-05-27** — Created `01_research/user-research/pi-interview-guide.md` — semi-structured interview guide for 2–3 academic PIs, designed to surface evidence without leading. Added `interview-guide` artifact type to manifest with full schema. New subfolder `01_research/user-research/`.
- **2026-05-27** — Added the "Speak researcher, not developer" rule to `00_meta/rules/design-rules.md` with a starter translation table (fork → Replicate/Adapt; snapshot → Saved version; module → Question/Item/Artifact; etc.). Internal docs keep developer-precise terms; user-facing surfaces translate. CLAUDE.md updated to reference the rule.
- **2026-05-27** — **ADR-0002 accepted** (`04_architecture/adrs/0002-forking-model.md`). Adopted stripped Option B: snapshot-based forking with pull-from-upstream deferred but forward-ready. Eight load-bearing principles: immutable version history per experiment; versions have a kind (autosave / named / preregistered / published); every version captures the full module-version lock set (per ADR-0001); forks reference a specific parent version, not an experiment in the abstract; forks are fully editable after creation; participant data never copied across forks; public-forkable default with per-experiment opt-out; frameworks share the model. Includes an 8-item forward-readiness checklist so upgrade to full B (pull-from-upstream, eventually merge-back) is feature-add, not migration. Validator clean (3 instances).
- **2026-05-27** — **ADR-0001 accepted** (`04_architecture/adrs/0001-modular-composition-theme-overlays.md`). Adopted versioned modular composition with theme overlays (Option C from the chat discussion). Five load-bearing principles: module identity is `source/key@version`; module schemas are first-class (Zod/JSON Schema); themes are pure-functional overlays (filter/configure, never mutate); forks reference module versions explicitly; module registry interface is plugin-ready (deferred plugin runtime). Module granularity = question type / artifact; blocks are compositions; frameworks are curated content. Calibration: project owner explicitly chose architectural flexibility + data safety + fork integrity over time-to-ship. Validator clean (2 instances).
- **2026-05-27** — Resolved 6 key product decisions in `02_product/product-brief.md`: positioning is "Qualtrics + GitHub for research" (combination is the wedge); thematic spaces are composable overlays on one everything-space (NOT separate products); AI is V1-architecture-ready / V2+ feature-bearing; chatbot surveys are V2 conversational rendering; curation is human (project owner); stats are narrow (G\*Power + manipulation-check + quick dashboard descriptives); AI synthesis is extraction (procedure + materials) not full generation. Reordered the ADR queue — ADR-0001 is now the modular composition / theme overlay model, the load-bearing decision.
- **2026-05-27** — Captured the raw product vision in `02_product/product-brief.md` — 12 themes from the project owner and others (GitHub-for-research, thematic spaces, AI-assisted authoring, built-in stats, MCP, qualitative support, etc.). Tagged as raw brainstorm, not validated. Includes scope analysis (V1 / V2 / V3 / "or never"), pushback on built-in stats and AI-synthesis ambitions, list of 5–6 ADRs the brief now triggers, and a 4-step decomposition plan (JTBDs, ADRs, research questions, persona expansion). Open positioning question surfaced: better-Qualtrics vs. GitHub-for-research.
- **2026-05-27** — Drafted the Principal Investigator persona (`02_product/personas/principal-investigator.md`) — Dr. Maya Okonkwo, academic PI in misinformation research. Single-source observational evidence; marked `draft` with explicit list of what to validate. Also extended the manifest schema to give `persona` a proper template, and fixed a parser bug where in-section bullets were being treated as top-level fields. Validator clean (`1 instance`).
- **2026-05-27** — Built the manifest system (`00_meta/manifest/`): YAML schema declares all 23 artifact types, regenerator writes templates from it, validator checks instances + template drift, scaffold helper creates new instances with required fields stubbed. Templates are now derived, not hand-edited. Updated CLAUDE.md with the new rules.
- **2026-05-27** — Fixed broken Send buttons in dashboard. Switched to copy-to-clipboard with toast notification (the artifact sandbox does not expose a way to inject messages into the Cowork chat, so direct send is not possible).
- **2026-05-27** — Built workflow dashboard artifact (`00_meta/dashboard.html`) + this STATUS.md mirror. Wired CLAUDE.md to keep both in sync.
- **2026-05-27** — Updated `STACK.md` with two-path framing (fast-to-ship vs frugal) + cost scaling table addressing the Vercel question.
- **2026-05-27** — Created workspace scaffold: 4 root docs, 6 rule files, 8 templates, 7 numbered phase folders with READMEs, archive folder.
